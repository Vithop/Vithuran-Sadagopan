# Project Guidelines & Rules

## Browser Automation
- **Default Engine**: Always use the built-in Antigravity browser automation (`browser_subagent`) for visual verification, UI testing, layout checking, and screenshots.
- **Playwright MCP Exception**: Only use Playwright MCP server tools when the task explicitly requires repeated, continuous programmatic browser scripting or consistent automation loops that need external script driver control.
